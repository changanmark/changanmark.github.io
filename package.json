{
  "name": "changanmark-blog-examples",
  "private": true,
  "workspaces": [
    "projects/*"
  ],
  "scripts": {
    "build:examples": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}

---
title: "WebGL2基本原理"
date: 2026-08-31 20:00:00 +0800
categories: [ "WebGL2", "webgl2fundamentals" ]
tags: [ "webgl2" ]
toc: true
---

{% include typescript-demo.html slug="hello-webgl2" title="WebGL2基本原理" height="520" %}

# WebGL2基本原理

WebGL是在GPU上运行的一个程序（Program），由两个函数组成，这两个函数分别叫做顶点着色器（Vertex Shader）和片段着色器（Fragment
Shader）。使用GLSL语言编写。

Vertex Shader函数的用途是计算顶点的位置，WebGL基于顶点着色器函数输出的顶点位置来栅格化不同类型的基本图元（点、线、三角形）。

Fragment Shader函数的用途是在栅格化图元的时候，计算当前正在绘制的图元的每个片段上的颜色。

WebGL是一个状态机，几乎所有的API都是为这对函数的运行设置状态。

## WebGL2的编程的基本顺序

1. 创建两个函数（VertexShader、FragmentShader），将他们链接成一个Program供GPU使用
2. 查找函数中接收数据的参数
3. 给参数上传数据
4. 调用WebGL绘制，根据上面的数据绘制图元

## 着色器接收数据的方式

1. 属性（Attribute）、缓冲区（Buffer）
2. Uniforms
3. 纹理（Textures）
4. Varyings

## 顶点着色器函数代码

```vertex shader
#version 300 es

in vec4 a_position;
void main() {
  gl_Position = a_position;
}
```

- #version 300 es 表示GLSL的版本，必须放在第一行
- in vec4 a_position; 这是一个Attribute变量，表示接收一个vector4类型的顶点属性数据（顶点位置）
- void main 就和C语言一样，是入口函数
- gl_Position = a_position; 是将外部传进来的顶点属性（位置）赋值给gl_Position。gl_Position是顶点着色器函数内置的特殊变量，表示输出的顶点位置。

总体来说就是根据输出，输出了顶点

## 片段着色器函数代码

```frag shader
#version 300 es

precision highp float;
out vec4 outColor;
void main() {
    outColor = vec4(1, 0, 0.5, 1);
}
```

- #version 300 es与顶点着色器一样是声明GLSL语言的版本
- precision highp float;表示精度
- out vec4 outColor; 片段着色器需要定义一个输出
- void main 就和C语言一样，是入口函数
- outColor = vec4(1, 0, 0.5, 1); 设置输出的颜色值，RGBA，范围是0-1

总体来说输出了颜色值

## 创建函数对象，链接成Program对象给GPU使用

```js
// 根据函数源码，创建对应的shader函数对象
function createShader(gl, type, source) {
  var shader = gl.createShader(type);
  gl.shaderSource(shader, source);
  gl.compileShader(shader);
  var success = gl.getShaderParameter(shader, gl.COMPILE_STATUS);
  if (success) {
    return shader;
  }

  console.log(gl.getShaderInfoLog(shader));
  gl.deleteShader(shader);
}

// 创建顶点着色器函数对象
var vertexShader = createShader(gl, gl.VERTEX_SHADER, vertexShaderSource);
// 创建片段着色器函数对象
var fragmentShader = createShader(gl, gl.FRAGMENT_SHADER, fragmentShaderSource);

// 把顶点着色器对象和片段着色器对象链接成Program
function createProgram(gl, vertexShader, fragmentShader) {
  var program = gl.createProgram();
  gl.attachShader(program, vertexShader);
  gl.attachShader(program, fragmentShader);
  gl.linkProgram(program);
  var success = gl.getProgramParameter(program, gl.LINK_STATUS);
  if (success) {
    return program;
  }

  console.log(gl.getProgramInfoLog(program));
  gl.deleteProgram(program);
}

// 创建程序对象
var program = createProgram(gl, vertexShader, fragmentShader);
```

这就已经在GPU上创建成功了程序，接下来就是给顶点着色器函数传递数据，让顶点着色器输出顶点位置，根据顶点位置绘制图元，然后根据片段着色器的颜色填充颜色。

上面创建的shader函数对象和链接的program都只是一个空壳，具体要绘制什么样的图形，还是看给他传递什么样的顶点位置

## 在javascript中传递数据

```javascript
// 查找属性变量
var positionAttributeLocation = gl.getAttribLocation(program, "a_position");
// 创建缓冲区
var positionBuffer = gl.createBuffer();
gl.bindBuffer(gl.ARRAY_BUFFER, positionBuffer);

var positions = [
  0, 0,
  0, 0.5,
  0.7, 0,
];
// 上传顶点信息到缓冲区
gl.bufferData(gl.ARRAY_BUFFER, new Float32Array(positions), gl.STATIC_DRAW);

var vao = gl.createVertexArray();
gl.bindVertexArray(vao);
gl.enableVertexAttribArray(positionAttributeLocation);

var size = 2;          // 2 components per iteration
var type = gl.FLOAT;   // the data is 32bit floats
var normalize = false; // don't normalize the data
var stride = 0;        // 0 = move forward size * sizeof(type) each iteration to get the next position
var offset = 0;        // start at the beginning of the buffer
// 设置属性变量在缓冲区中取值的方式
gl.vertexAttribPointer(
  positionAttributeLocation, size, type, normalize, stride, offset)

webglUtils.resizeCanvasToDisplaySize(gl.canvas);
gl.viewport(0, 0, gl.canvas.width, gl.canvas.height);
gl.useProgram(program);
gl.bindVertexArray(vao);

var primitiveType = gl.TRIANGLES;
var offset = 0;
var count = 3;
// 绘制图元
gl.drawArrays(primitiveType, offset, count);
```

## 裁剪空间坐标

WebGL中VertexShader输出的位置必须是裁剪空间范围 -1 ~ 1 。所以如果你的顶点数据不在这个范围内，就要转换到这个范围内。

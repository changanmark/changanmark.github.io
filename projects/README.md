# TypeScript 示例工程

三个示例都是可以单独开发和构建的 Vite + TypeScript 工程：

- geometry-material-mesh
- scene-tree
- material

在仓库根目录安装依赖并构建：

    npm install
    npm run build:examples

单独开发一个示例：

    npm run dev:geometry
    npm run dev:scene-tree
    npm run dev:material

构建结果统一生成到 examples/工程名，由 Jekyll 发布。
